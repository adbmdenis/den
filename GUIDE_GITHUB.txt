╔══════════════════════════════════════════════════════════════════╗
║              GUIDE RAPIDE - PUSH VERS GITHUB                     ║
╚══════════════════════════════════════════════════════════════════╝

┌──────────────────────────────────────────────────────────────────┐
│ 🎯 OBJECTIF                                                       │
└──────────────────────────────────────────────────────────────────┘

Pousser le code du dossier serveur_iptv vers:
https://github.com/adbmdenis/den

┌──────────────────────────────────────────────────────────────────┐
│ ⚡ MÉTHODE RAPIDE (AUTOMATIQUE)                                   │
└──────────────────────────────────────────────────────────────────┘

1. Double-cliquer sur: push_to_github.bat
2. Suivre les instructions
3. Entrer vos identifiants GitHub quand demandé

┌──────────────────────────────────────────────────────────────────┐
│ 📝 MÉTHODE MANUELLE (ÉTAPE PAR ÉTAPE)                            │
└──────────────────────────────────────────────────────────────────┘

Ouvrir un terminal dans le dossier serveur_iptv et exécuter:

1. Initialiser Git
   git init

2. Configurer Git (remplacer par vos infos)
   git config user.name "adbmdenis"
   git config user.email "votre@email.com"

3. Ajouter tous les fichiers
   git add .

4. Faire le premier commit
   git commit -m "Initial commit - Serveur IPTV complet"

5. Créer la branche main
   git branch -M main

6. Ajouter le remote
   git remote add origin https://github.com/adbmdenis/den.git

7. Pousser vers GitHub
   git push -u origin main

┌──────────────────────────────────────────────────────────────────┐
│ 🔑 AUTHENTIFICATION GITHUB                                        │
└──────────────────────────────────────────────────────────────────┘

GitHub ne supporte plus les mots de passe classiques.
Vous devez utiliser un Personal Access Token (PAT).

CRÉER UN TOKEN:

1. Aller sur: https://github.com/settings/tokens
2. Cliquer: "Generate new token" → "Generate new token (classic)"
3. Donner un nom: "IPTV Server"
4. Cocher les permissions:
   ☑ repo (tous)
   ☑ workflow
5. Cliquer: "Generate token"
6. COPIER LE TOKEN (vous ne le reverrez plus!)

UTILISER LE TOKEN:

Quand Git demande le mot de passe, collez le TOKEN au lieu du mot
de passe.

Username: adbmdenis
Password: ghp_xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx (votre token)

┌──────────────────────────────────────────────────────────────────┐
│ ⚠️ SI LE DÉPÔT EXISTE DÉJÀ                                       │
└──────────────────────────────────────────────────────────────────┘

Si vous avez déjà créé le dépôt sur GitHub avec un README:

Option 1: Forcer le push (écrase le contenu distant)
   git push -u origin main --force

Option 2: Pull d'abord, puis push
   git pull origin main --allow-unrelated-histories
   git push -u origin main

Option 3: Supprimer et recréer le dépôt sur GitHub
   1. Aller sur https://github.com/adbmdenis/den
   2. Settings → Danger Zone → Delete this repository
   3. Recréer un nouveau dépôt vide
   4. Relancer push_to_github.bat

┌──────────────────────────────────────────────────────────────────┐
│ 📁 FICHIERS QUI SERONT POUSSÉS                                    │
└──────────────────────────────────────────────────────────────────┘

✅ Code source:
   - server.py
   - admin_panel.py
   - database.py
   - multi_service.py
   - config.py

✅ Documentation:
   - README.md
   - GUIDE_HEBERGEMENT.md
   - COMPARAISON_HEBERGEURS.txt
   - STATUS_FINAL.md
   - ACCES_RAPIDE.txt

✅ Scripts:
   - fix_superadmin_password.py
   - test_login_simple.py
   - REDEMARRER_MAINTENANT.bat

✅ Configuration:
   - requirements.txt
   - .gitignore

❌ Fichiers exclus (.gitignore):
   - database.db (base de données)
   - __pycache__/ (cache Python)
   - *.log (logs)
   - test_*.py (fichiers de test)
   - debug_*.py (fichiers de debug)

┌──────────────────────────────────────────────────────────────────┐
│ 🔄 MISES À JOUR FUTURES                                           │
└──────────────────────────────────────────────────────────────────┘

Pour pousser des modifications ultérieures:

1. Ajouter les fichiers modifiés
   git add .

2. Commiter les changements
   git commit -m "Description des modifications"

3. Pousser vers GitHub
   git push

┌──────────────────────────────────────────────────────────────────┐
│ 🐛 RÉSOLUTION DE PROBLÈMES                                        │
└──────────────────────────────────────────────────────────────────┘

PROBLÈME: "fatal: remote origin already exists"
SOLUTION:
   git remote remove origin
   git remote add origin https://github.com/adbmdenis/den.git

PROBLÈME: "error: failed to push some refs"
SOLUTION:
   git pull origin main --allow-unrelated-histories
   git push -u origin main

PROBLÈME: "Authentication failed"
SOLUTION:
   - Vérifiez votre username
   - Utilisez un Personal Access Token au lieu du mot de passe
   - Créez un token sur: https://github.com/settings/tokens

PROBLÈME: "Permission denied"
SOLUTION:
   - Vérifiez que vous êtes le propriétaire du dépôt
   - Vérifiez les permissions du token

┌──────────────────────────────────────────────────────────────────┐
│ ✅ VÉRIFICATION APRÈS PUSH                                        │
└──────────────────────────────────────────────────────────────────┘

1. Aller sur: https://github.com/adbmdenis/den
2. Vérifier que tous les fichiers sont présents
3. Vérifier que le README.md s'affiche correctement
4. Tester le clone:
   git clone https://github.com/adbmdenis/den.git test_clone
   cd test_clone
   python server.py

┌──────────────────────────────────────────────────────────────────┐
│ 📊 COMMANDES GIT UTILES                                           │
└──────────────────────────────────────────────────────────────────┘

Voir le statut:
   git status

Voir l'historique:
   git log --oneline

Voir les fichiers suivis:
   git ls-files

Voir les remotes:
   git remote -v

Annuler le dernier commit (garder les modifications):
   git reset --soft HEAD~1

Voir les différences:
   git diff

┌──────────────────────────────────────────────────────────────────┐
│ 🎯 PROCHAINES ÉTAPES APRÈS LE PUSH                                │
└──────────────────────────────────────────────────────────────────┘

1. ✅ Ajouter une description au dépôt sur GitHub
2. ✅ Ajouter des topics: iptv, python, flask, admin-panel
3. ✅ Créer une release (v1.0.0)
4. ✅ Ajouter une licence (MIT recommandée)
5. ✅ Activer GitHub Pages (optionnel)
6. ✅ Configurer GitHub Actions pour CI/CD (optionnel)

┌──────────────────────────────────────────────────────────────────┐
│ 🌟 RENDRE LE DÉPÔT ATTRACTIF                                      │
└──────────────────────────────────────────────────────────────────┘

Sur GitHub, ajouter:

1. Description courte:
   "Système complet de gestion IPTV avec panel admin"

2. Topics (tags):
   - iptv
   - python
   - flask
   - admin-panel
   - m3u
   - streaming
   - playlist

3. Website:
   Votre URL de démo (si vous en avez une)

4. Badges dans README.md:
   - Python version
   - License
   - Stars
   - Forks

╔══════════════════════════════════════════════════════════════════╗
║  PRÊT À POUSSER? Exécutez: push_to_github.bat                    ║
╚══════════════════════════════════════════════════════════════════╝
